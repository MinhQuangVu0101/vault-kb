# vault-kb

Local MCP server that makes opt-in Obsidian notes searchable by AI tools (FTS5 + semantic + link graph). Plain-JS ESM, Node >= 20. No linter; type safety comes from tsc checkJs over JSDoc annotations.

## Health Stack

- typecheck: npm run typecheck
- test: npm test
